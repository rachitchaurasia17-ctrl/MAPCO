import { escapeHtml as h } from './domain';
import { options } from './templates';
export function provisioningForm(key: string): string {
  return `<section><h2>Create dealer account</h2><p>Seven days of trial access from account creation. Four approved devices. Existing dealer data stays in the same account when you record paid access later.</p><form data-form="provision" data-key="${h(key)}"><div class="fc-grid"><label>Business name<input name="businessName" required minlength="2" maxlength="160"></label><label>Dealer ID<input name="dealerId" required pattern="[a-z0-9][a-z0-9-]{1,38}[a-z0-9]" placeholder="chaurasia-properties"><small>Unique lowercase letters, numbers and hyphens.</small></label><label>Owner name<input name="ownerName" required minlength="2" maxlength="120"></label><label>Owner phone<input name="ownerPhone" type="tel" maxlength="40"></label><label>Primary area<input name="primaryArea" required minlength="2" maxlength="120"></label><label>Login email<input name="loginEmail" type="email" required maxlength="254" autocomplete="off"></label><label>Initial login password<input name="password" type="password" required minlength="8" maxlength="72" autocomplete="new-password"><small>Share it privately with the dealer. It is never stored by this dashboard.</small></label><label>How you found this dealer<select name="source" required><option value="">Choose a source</option>${options(['family','referral','cold','walk_in','inbound'])}</select></label><label>Pitch version<input name="pitch" required maxlength="60" placeholder="Name of the pitch you used"></label><label>Trial protocol<input name="protocol" required maxlength="60" placeholder="Name of the trial process"></label></div><div class="fc-row"><button>Create seven-day trial</button><button type="button" class="secondary" data-cancel-provision>Cancel</button></div></form></section>`;
}
