# Raw source-map working folders are local inputs, not production assets.
/maps with svg/
/normal maps/

.env
.env.local
.env.*.local

# Curated landmark staging folder — operator drop point.
# The canonical MAPCO-owned copies are published to v2/public/landmarks/
# by scripts/import-landmarks.mjs (byte-identical), so this is not tracked.
landmarks photos/

# Accidental nested repo clone in templates
templates/MAPCO/

# Temporary patch drop zips
founders thing*.zip
founderthing*.zip
